---
title: "SQL Joins Explicados com Exemplos"
description: "Aprenda os diferentes tipos de JOINs em SQL com exemplos práticos e diagramas visuais."
category: "Banco de Dados"
tags:
  - SQL
  - Banco de Dados
  - PostgreSQL
  - MySQL
featured: true
publishedAt: "2026-05-25"
---

## Introdução

JOINs são cláusulas SQL usadas para combinar registros de duas ou mais tabelas baseadas em uma coluna relacionada entre elas.

## INNER JOIN

Retorna apenas os registros com correspondência em ambas as tabelas:

```mermaid
flowchart LR
    U[(users)] -- "INNER JOIN
    u.id = o.user_id" --> O[(orders)]
    style U fill:#8b5cf6,color:#fff
    style O fill:#3b82f6,color:#fff
```

```sql
SELECT u.name, o.order_date, o.total
FROM users u
INNER JOIN orders o ON u.id = o.user_id;
```

| name | order_date | total |
|------|------------|-------|
| João | 2026-01-15 | 250.00 |
| Maria | 2026-01-16 | 180.00 |

## LEFT JOIN

Retorna todos os registros da tabela esquerda e os correspondentes da direita:

```mermaid
flowchart LR
    U[(users)] -- "LEFT JOIN
    todos os users" --> O[(orders)]
    U -.->|NULL se sem pedido| NO[NULL]
    style U fill:#8b5cf6,color:#fff,stroke-width:3px
    style O fill:#3b82f6,color:#fff
    style NO fill:#ef4444,color:#fff
```

```sql
SELECT u.name, o.order_date, o.total
FROM users u
LEFT JOIN orders o ON u.id = o.user_id;
```

## RIGHT JOIN

Retorna todos os registros da tabela direita e os correspondentes da esquerda:

```mermaid
flowchart LR
    U[(users)] -- "RIGHT JOIN
    todos os orders" --> O[(orders)]
    O -.->|NULL se sem usuário| NU[NULL]
    style U fill:#8b5cf6,color:#fff
    style O fill:#3b82f6,color:#fff,stroke-width:3px
    style NU fill:#ef4444,color:#fff
```

```sql
SELECT u.name, o.order_date, o.total
FROM users u
RIGHT JOIN orders o ON u.id = o.user_id;
```

## FULL OUTER JOIN

Retorna todos os registros quando há correspondência em qualquer uma das tabelas:

```mermaid
flowchart LR
    U[(users)] -- "FULL OUTER JOIN
    todos os registros" --> O[(orders)]
    U -.->|"NULL (sem match)"| X1[ ]
    O -.->|"NULL (sem match)"| X2[ ]
    style U fill:#8b5cf6,color:#fff
    style O fill:#3b82f6,color:#fff
    style X1 fill:#ef4444,color:#fff
    style X2 fill:#ef4444,color:#fff
```

```sql
SELECT u.name, o.order_date
FROM users u
FULL OUTER JOIN orders o ON u.id = o.user_id;
```

## Dicas de Performance

| Tipo | Performance | Uso Recomendado |
|------|-------------|-----------------|
| INNER | Ótima | Consultas mais comuns |
| LEFT | Boa | Quando precisa de todos os registros da esquerda |
| RIGHT | Boa | Quando precisa de todos os registros da direita |
| FULL | Mais lenta | Casos específicos de auditoria |

## Conclusão

Entender JOINs é fundamental para qualquer pessoa que trabalhe com bancos de dados relacionais. Pratique com diferentes cenários para dominar o conceito.
